---
import Layout from "../layouts/Layout.astro";
import Animation from "../components/Animation.astro";
---

<Layout
  title="Contacto"
  description="Contáctanos para reservar tu scooter eléctrico o solicitar más información"
>
  <script src="../scripts/contact-form.js"></script>
  <script src="../scripts/image-viewer.js"></script>

  <!-- Hero Section -->
  <section class="relative overflow-hidden py-24 bg-dark">
    <!-- Fondo Dinámico -->
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-primary/10 via-dark to-dark opacity-40"
    >
    </div>
    <div
      class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-5 bg-[length:40px_40px]"
    >
    </div>

    <div class="container mx-auto px-4 relative z-10 text-center">
      <Animation animation="fade-down">
        <h1 class="text-4xl md:text-6xl font-bold mb-6 text-white">
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-secondary"
            >Contáctanos</span
          >
        </h1>
      </Animation>
      <Animation animation="fade-up" delay={200}>
        <p class="text-xl text-gray-300 max-w-2xl mx-auto">
          Estamos listos para ayudarte a planear tu próxima aventura en el
          Malecón.
        </p>
      </Animation>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-16 bg-dark relative">
    <div class="container mx-auto px-4 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
        <!-- Contact Form -->
        <Animation animation="fade-right">
          <div
            class="bg-white/5 backdrop-blur-xl p-8 rounded-3xl border border-white/10 shadow-2xl relative overflow-hidden"
          >
            <div
              class="absolute top-0 right-0 w-32 h-32 bg-primary/10 rounded-full blur-[50px] -mr-16 -mt-16"
            >
            </div>

            <h2 class="text-2xl font-bold mb-6 text-white relative z-10">
              Envíanos un mensaje
            </h2>
            <form
              id="contactForm"
              action="https://formsubmit.co/kaminatrigger@gmail.com"
              method="POST"
              class="space-y-6 relative z-10"
            >
              <!-- Campos ocultos -->
              <input
                type="hidden"
                name="_subject"
                value="Nueva reserva de ScooterCoop"
              />
              <input type="hidden" name="_template" value="table" />
              <input type="hidden" name="_captcha" value="false" />
              <input
                type="hidden"
                name="_next"
                value="https://scootercoop.com/gracias"
              />

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label for="nombre" class="block text-gray-400 mb-2 text-sm"
                    >Nombre</label
                  >
                  <input
                    type="text"
                    id="nombre"
                    name="nombre"
                    class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                    required
                  />
                </div>
                <div>
                  <label for="apellido" class="block text-gray-400 mb-2 text-sm"
                    >Apellido</label
                  >
                  <input
                    type="text"
                    id="apellido"
                    name="apellido"
                    class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                    required
                  />
                </div>
              </div>

              <div>
                <label for="email" class="block text-gray-400 mb-2 text-sm"
                  >Correo Electrónico</label
                >
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                  required
                />
              </div>

              <div>
                <label for="telefono" class="block text-gray-400 mb-2 text-sm"
                  >Teléfono</label
                >
                <input
                  type="tel"
                  id="telefono"
                  name="telefono"
                  class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                />
              </div>

              <div>
                <label for="servicio" class="block text-gray-400 mb-2 text-sm"
                  >Servicio de Interés</label
                >
                <select
                  id="servicio"
                  name="servicio"
                  class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all appearance-none"
                >
                  <option value="" class="bg-dark"
                    >Selecciona un servicio</option
                  >
                  <option value="30min" class="bg-dark"
                    >Alquiler 30 minutos</option
                  >
                  <option value="40min" class="bg-dark"
                    >Alquiler 40 minutos</option
                  >
                  <option value="60min" class="bg-dark">Alquiler 1 hora</option>
                  <option value="grupo" class="bg-dark">Tour grupal</option>
                  <option value="evento" class="bg-dark">Evento especial</option
                  >
                </select>
              </div>

              <div>
                <label for="cantidad" class="block text-gray-400 mb-2 text-sm"
                  >Cantidad de Patinetas</label
                >
                <input
                  type="number"
                  id="cantidad"
                  name="cantidad"
                  min="1"
                  value="1"
                  class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                />
              </div>

              <!-- Total Estimado -->
              <div
                id="totalContainer"
                class="hidden p-4 bg-white/5 rounded-xl border border-white/10"
              >
                <div class="flex justify-between items-center">
                  <span class="text-gray-300">Total Estimado:</span>
                  <span id="totalPrice" class="text-2xl font-bold text-primary"
                    >$0 COP</span
                  >
                </div>
                <p id="totalNote" class="text-xs text-gray-500 mt-1 text-right">
                </p>
              </div>

              <div>
                <label for="mensaje" class="block text-gray-400 mb-2 text-sm"
                  >Mensaje</label
                >
                <textarea
                  id="mensaje"
                  name="mensaje"
                  rows="4"
                  class="w-full px-4 py-3 bg-dark/50 border border-white/10 rounded-xl text-white focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-all"
                  required></textarea>
              </div>

              <div id="statusMessage" class="hidden mb-4 text-sm"></div>

              <button
                type="submit"
                class="w-full bg-gradient-to-r from-primary to-secondary text-white font-bold py-4 px-6 rounded-xl hover:shadow-[0_0_20px_rgba(0,240,255,0.4)] transition-all transform hover:scale-[1.02]"
              >
                Enviar Mensaje
              </button>
            </form>
          </div>
        </Animation>

        <!-- Contact Info -->
        <Animation animation="fade-left" delay={200}>
          <div class="space-y-8">
            <div>
              <h2 class="text-2xl font-bold mb-8 text-white">
                Información de Contacto
              </h2>
              <div class="space-y-6">
                <div class="flex items-start group">
                  <div
                    class="w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center mr-4 text-primary group-hover:bg-primary/20 transition-colors"
                  >
                    <i class="bx bx-map text-2xl"></i>
                  </div>
                  <div>
                    <h3 class="font-bold text-lg text-white mb-1">Dirección</h3>
                    <p class="text-gray-400">
                      Malecón del Río, Barranquilla, Colombia
                    </p>
                    <p class="text-gray-500 text-sm">Vía 40 con calle 79</p>
                  </div>
                </div>

                <div class="flex items-start group">
                  <div
                    class="w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center mr-4 text-primary group-hover:bg-primary/20 transition-colors"
                  >
                    <i class="bx bx-phone text-2xl"></i>
                  </div>
                  <div>
                    <h3 class="font-bold text-lg text-white mb-1">Teléfono</h3>
                    <p class="text-gray-400">+57 300 123 4567</p>
                    <p class="text-gray-400">+57 5 123 4567</p>
                  </div>
                </div>

                <div class="flex items-start group">
                  <div
                    class="w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center mr-4 text-primary group-hover:bg-primary/20 transition-colors"
                  >
                    <i class="bx bx-envelope text-2xl"></i>
                  </div>
                  <div>
                    <h3 class="font-bold text-lg text-white mb-1">Email</h3>
                    <p class="text-gray-400">scootercoopinfo@gmail.com</p>
                  </div>
                </div>

                <div class="flex items-start group">
                  <div
                    class="w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center mr-4 text-primary group-hover:bg-primary/20 transition-colors"
                  >
                    <i class="bx bx-time text-2xl"></i>
                  </div>
                  <div>
                    <h3 class="font-bold text-lg text-white mb-1">
                      Horario de Atención
                    </h3>
                    <p class="text-gray-400">
                      Lunes a Viernes: 3:00 PM - 10:00 PM
                    </p>
                    <p class="text-gray-400">
                      Sábados y Domingos: 2:00 PM - 11:00 PM
                    </p>
                  </div>
                </div>
              </div>
            </div>

            <!-- Social Media -->
            <div>
              <h2 class="text-2xl font-bold mb-6 text-white">Síguenos</h2>
              <div class="flex space-x-4">
                <a
                  href="#"
                  class="w-12 h-12 rounded-full bg-white/5 flex items-center justify-center text-white hover:bg-primary hover:text-dark transition-all duration-300"
                >
                  <i class="bx bxl-facebook text-2xl"></i>
                </a>
                <a
                  href="https://www.instagram.com/scootercoop_/"
                  class="w-12 h-12 rounded-full bg-white/5 flex items-center justify-center text-white hover:bg-primary hover:text-dark transition-all duration-300"
                >
                  <i class="bx bxl-instagram text-2xl"></i>
                </a>
                <a
                  href="#"
                  class="w-12 h-12 rounded-full bg-white/5 flex items-center justify-center text-white hover:bg-primary hover:text-dark transition-all duration-300"
                >
                  <i class="bx bxl-whatsapp text-2xl"></i>
                </a>
              </div>
            </div>
          </div>
        </Animation>
      </div>
    </div>
  </section>

  <!-- Map Section -->
  <section class="py-16 bg-dark border-t border-white/5">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12 text-white">
        Nuestra Ubicación
      </h2>
      <div class="bg-white/5 p-2 rounded-3xl border border-white/10 shadow-2xl">
        <div class="bg-dark h-96 rounded-2xl overflow-hidden relative group">
          <img
            src="/mapa.jpg"
            alt="Mapa de ubicación en el Malecón del Río, Barranquilla"
            class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-500"
          />
          <div
            class="absolute inset-0 flex items-center justify-center pointer-events-none"
          >
            <div
              class="bg-dark/80 backdrop-blur-sm px-6 py-3 rounded-full border border-primary/50 text-primary font-bold flex items-center gap-2"
            >
              <i class="bx bx-map-pin text-2xl animate-bounce"></i>
              Encuéntranos aquí
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>
