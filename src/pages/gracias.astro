---
import Layout from "../layouts/Layout.astro";
import Animation from "../components/Animation.astro";
---

<Layout
  title="Gracias por tu mensaje"
  description="Hemos recibido tu mensaje y nos pondremos en contacto contigo pronto"
>
  <!-- Hero Section -->
  <section
    class="relative h-screen flex items-center justify-center overflow-hidden bg-dark"
  >
    <!-- Fondo Dinámico -->
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-primary/20 via-dark to-dark opacity-60"
    >
    </div>
    <div
      class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-5 bg-[length:40px_40px]"
    >
    </div>

    <div class="container mx-auto px-4 relative z-10 text-center">
      <Animation animation="zoom-in">
        <div
          class="max-w-2xl mx-auto bg-white/5 backdrop-blur-xl p-12 rounded-[3rem] border border-white/10 shadow-[0_0_50px_rgba(0,240,255,0.2)]"
        >
          <div
            class="w-24 h-24 rounded-full bg-gradient-to-r from-primary to-secondary flex items-center justify-center mx-auto mb-8 shadow-lg shadow-primary/50 animate-pulse"
          >
            <i class="bx bx-check text-5xl text-white"></i>
          </div>

          <h1 class="text-4xl md:text-5xl font-bold mb-6 text-white">
            ¡Mensaje Enviado!
          </h1>
          <p class="text-xl text-gray-300 mb-8">
            Hemos recibido tu solicitud correctamente. Nuestro equipo te
            contactará muy pronto para confirmar los detalles.
          </p>

          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="/"
              class="px-8 py-4 bg-white text-dark font-bold rounded-full hover:bg-primary hover:text-white transition-all shadow-lg hover:shadow-primary/50"
            >
              Volver al Inicio
            </a>
            <a
              href="/galeria"
              class="px-8 py-4 bg-transparent border border-white/30 text-white font-bold rounded-full hover:bg-white/10 transition-all"
            >
              Ver Galería
            </a>
          </div>
        </div>
      </Animation>
    </div>
  </section>
</Layout>
